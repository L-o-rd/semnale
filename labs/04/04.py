# f in {40 Hz, ..., 200 Hz}
# fc (f caracteristica) <= 200 Hz
# fs >= 2 * fc
# fs >= 2 * 200 Hz
# fs >= 400 Hz
# min(fs) = 400 Hz
# Daca luam fs ca sa fie garantat indiferent de
# echipament, atunci fs > 400 Hz (+ epsilon)

exit()